@model IEnumerable<PredictionHistory>
<h2>Istoric predicții</h2>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Data
                <a asp-action="History"
                   asp-route-paymentType="@ViewBag.CurrentPaymentType"
                   asp-route-minPrice="@ViewBag.CurrentMinPrice"
                   asp-route-maxPrice="@ViewBag.CurrentMaxPrice"
                   asp-route-minDate="@ViewBag.CurrentMinDate"
                   asp-route-maxDate="@ViewBag.CurrentMaxDate"
                   asp-route-sortOrder="price_asc">↑</a>
                <a asp-action="History"
                   asp-route-paymentType="@ViewBag.CurrentPaymentType"
                   asp-route-minPrice="@ViewBag.CurrentMinPrice"
                   asp-route-maxPrice="@ViewBag.CurrentMaxPrice"
                   asp-route-minDate="@ViewBag.CurrentMinDate"
                   asp-route-maxDate="@ViewBag.CurrentMaxDate"
                   asp-route-sortOrder="price_desc">↓</a>
            </th>
            <th>Pasageri</th>
            <th>Distanță</th>
            <th>Timp (sec)</th>
            <th>Tip plată</th>
            <th>Preț prezis
                <a asp-action="History"
                   asp-route-paymentType="@ViewBag.CurrentPaymentType"
                   asp-route-minPrice="@ViewBag.CurrentMinPrice"
                   asp-route-maxPrice="@ViewBag.CurrentMaxPrice"
                   asp-route-minDate="@ViewBag.CurrentMinDate"
                   asp-route-maxDate="@ViewBag.CurrentMaxDate"
                   asp-route-sortOrder="price_asc">↑</a>
                <a asp-action="History"
                   asp-route-paymentType="@ViewBag.CurrentPaymentType"
                   asp-route-minPrice="@ViewBag.CurrentMinPrice"
                   asp-route-maxPrice="@ViewBag.CurrentMaxPrice"
                   asp-route-minDate="@ViewBag.CurrentMinDate"
                   asp-route-maxDate="@ViewBag.CurrentMaxDate"
                   asp-route-sortOrder="price_desc">↓</a>
            </th>      
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.CreatedAt</td>
                <td>@item.PassengerCount</td>
                <td>@item.TripDistance</td>
                <td>@item.TripTimeInSecs</td>
                <td>@item.PaymentType</td>
                <td>@item.PredictedPrice</td>
            </tr>
        }
    </tbody>
</table> 

<form method="get" asp-action="History" class="row g-3 mb-3">
    <div class="col-md-3">
        <label class="form-label">Tip plată</label>
        <select name="paymentType" class="form-select">
            <option value="">Toate</option>
            <option value="CRD" selected="@(ViewBag.PaymentType == "CRD")">Card</option>
            <option value="CSH" selected="@(ViewBag.PaymentType == "CSH")">Cash</option>
            <option value="OTHER" selected="@(ViewBag.PaymentType == "OTHER")">Altele</option>
        </select>
    </div>
    <div class="col-md-2">
        <label class="form-label">Preț minim</label>
        <input type="number" step="0.01" name="minPrice"
               value="@(ViewBag.CurrentMinPrice ?? "")"
               class="form-control" />
    </div>
    <div class="col-md-2">
        <label class="form-label">Preț maxim</label>
        <input type="number" step="0.01" name="maxPrice"
               value="@(ViewBag.CurrentMaxPrice ?? "")"
               class="form-control" />
    </div>
    <div>
    <div class="col-md-3">
            <label class="form-label">Data minima</label>
            <input type="date" name="minDate"
                   value="@(ViewBag.CurrentMinDate ?? "")"
                   class="form-control" />
    </div>
    <div class="col-md-3">
                <label class="form-label">Data maxima</label>
                <input type="date" name="maxDate"
                       value="@(ViewBag.CurrentMaxDate ?? "")"
                       class="form-control" />
    </div>
    </div>
    <div class="col-md-2 align-self-end">
        <button type="submit" class="btn btn-primary">Filtrează</button>
    </div>
</form>
